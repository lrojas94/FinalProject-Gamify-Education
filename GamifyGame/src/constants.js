/*
IMPORTANT:
  The token here is fixed because of testing purposes. The game is supposed to have a login and shouls set the token
  dinamically, however, due to time constraints, it is better to just use a known token.
 */
export default {
  API_URL : 'http://localhost:3000/api/'
};
